# Report the Julia version used by clade

Report the Julia version used by clade

## Usage

``` r
julia_version()
```

## Value

Character scalar, e.g. `"1.10.0"`, or `NA` if Julia has not been started
yet.
