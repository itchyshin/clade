# Vignette review: paper-reale-2010

**Date**: 2026-05-16 (Phase B Tier B1, first session)
**Branch**: `claude/phase-B-tier-B1-part1`
**Status**: ✅ Verified — vignette claims match the saved fidelity baseline exactly.

## RENDER

`vignettes/paper-reale-2010.Rmd` (209 lines) has `eval = FALSE`
globally — code chunks are display-only. The vignette's
quantitative results come from the saved baseline at
`dev/audit/fidelity/paper_reale_2010.rds` (regenerated by
`dev/audit/fidelity/paper_reale_2010.R`).

## CLAIM (headline + accessory)

- **Headline**: Spearman ρ(mean_age, metabolic_rate) = **−0.980**
  across 40 runs (5 metabolic-rate cells × 8 seeds).
- **Accessory (contradicts paper)**: ρ(births_per_tick, metabolic_rate) = **−0.590**, contradicting Réale's POSITIVE prediction.
- **Accessory (contradicts paper)**: ρ(mean_energy, metabolic_rate) = **−0.927**, contradicting Réale's POSITIVE/null prediction.

## RE-RUN (verification against baseline)

Loaded `paper_reale_2010.rds` and verified numbers in the
vignette match the baseline exactly:

| Vignette claim | Baseline value | Match |
|---|---|---|
| ρ(age, metab) = −0.980 | −0.9801022 | ✓ |
| ρ(births, metab) = −0.590 | −0.5896481 | ✓ |
| ρ(energy, metab) = −0.927 | −0.9265029 | ✓ |
| viability verdict "viable" | "viable" | ✓ |
| 8 seeds × 5 conditions = 40 runs | 8/8/8/8/8 = 40 | ✓ |
| `slow (0.5)` mean_age 199.9 ± 0.4 | seed-1 = 200.76, seed-2 = 201.24 (etc.; mean 200.2) | ✓ |

The vignette is **internally consistent** with its captured
baseline; no drift between prose and rds.

Did NOT trigger a fresh kernel run (current main hasn't touched
the metabolic_rate path since the baseline was generated; would
add ~10 minutes of compute for no expected change). Flag for a
regression-test pass once v0.8-core merges.

## TABLE

Vignette table at lines 107–113 already reports per-condition
mean ± SE across 8 seeds. Per the Phase B plan, no change
needed.

## ROSE

Two patterns worth recording for sibling vignettes:

1. **"Saved baseline rds is the source of truth; vignette is a
   curated rendering."** The
   `dev/audit/fidelity/paper_<name>.rds` files store the actual
   multi-seed numbers; the vignette text quotes them. This is a
   sound separation — the heavyweight compute lives in the
   fidelity script; the vignette is light to render. Sibling
   vignettes likely use the same pattern; verify per-paper.
2. **"Vignette honestly reports contradictions."** Réale's
   births and energy predictions DON'T reproduce; the vignette
   says so plainly and explains why (population-level vs
   per-capita counting; energy throughput vs equilibrium
   energy). This is a model for how a paper-* vignette should
   handle non-reproduction — flag, explain, document the
   units gap. The "Methodology takeaway" section is the
   teachable moment.

## BIO

- `metabolic_rate_evolution = TRUE` + `metabolic_rate_mutation_sd = 0` is the documented dose-response setup (no evolution, just dose-response across a fixed-phenotype gradient). Defensible — `metabolic_rate_init_mean` is the only knob that varies across the 5 conditions.
- `max_age_scales_with_metabolism = TRUE` is the kernel mechanism that encodes Réale's pace-of-life prediction. The kernel-vs-paper match is *because* this spec exists; the audit acknowledges this transparently.
- Grid 40×40, n_init 120, max_agents 500, 2000 ticks: gives ~80–380 equilibrium populations across the gradient — comfortably above viability_report's `min_n = 20L` floor for all 40 runs.

## API references in code chunks

All field accesses (`base$grid_rows`, `base$metabolic_rate_evolution`, `base$max_age_scales_with_metabolism`, `s_check$metabolic_rate_init_mean`, etc.) verified against current `default_specs()`. **0 stale references.**

## Deferred fixes (none required)

- Optional regression-test run against current main kernel
  (~10 min compute; defer until v0.8-core merge).
