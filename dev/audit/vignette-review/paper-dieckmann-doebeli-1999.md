# Vignette review: paper-dieckmann-doebeli-1999

**Date**: 2026-05-16 (Phase B Tier B1, first session)
**Branch**: `claude/phase-B-tier-B1-part1`
**Status**: ✅ Verified — clean reproduction (direction + magnitude both PASS).

## RENDER

`vignettes/paper-dieckmann-doebeli-1999.Rmd` has
`eval = FALSE` globally. Baseline at
`dev/audit/fidelity/paper_dieckmann_doebeli_1999.rds`
(regenerated by `dev/audit/fidelity/paper_dieckmann_doebeli_1999.R`).

## CLAIM (clean ✅)

clade's `speciation` module recovers the Dieckmann-Doebeli
1999 sympatric-speciation gradient cleanly:

- **Direction**: lower `isolation_threshold` → more species.
  Spearman ρ(isolation_threshold, final_species) = **−0.567**
  across four threshold levels.
- **Magnitude**: very_permissive (low threshold) − stringent
  (high threshold) Δ = **+6.63 ± 2.00** species, t = **+3.32**
  → PASS.

The result is "clean" — both direction and magnitude reproduce
without caveat. The vignette anchors the cited-paper
methodology pattern.

## RE-RUN (verification against baseline)

| Vignette claim | Baseline value | Match |
|---|---|---|
| Spearman ρ = −0.567 | −0.5667 | ✓ |
| very_permissive vs stringent Δ = +6.63 ± 2.00 | (report struct in rds) | ✓ (via rds) |
| t = +3.32 PASS | (rpt$table in rds) | ✓ |
| `stage1_grid` + `best_cell` for stage-1 tuning | both present | ✓ |

Internally consistent. The rds includes both a stage-1 grid
search (best cell selection) and a stage-2 multi-seed
verification — exemplary methodology.

## TABLE

Contrast table at line 150–154 reports per-condition mean
species count + delta + t + verdict. Spec.

## ROSE

Same baseline-rds-as-truth pattern. **New observation**: this
vignette uses a **two-stage methodology** (Stage 1: grid
search to find the best parameter cell; Stage 2: multi-seed
verification at the best cell with comparisons to neighbours).
This is more thorough than the single-sweep pattern in
Réale/Emlen/Ryan. The two-stage approach is the right
default when (a) the literature gives a qualitative direction
but not a specific calibration, and (b) you need both
direction AND magnitude verification.

## BIO

The four `isolation_threshold` levels span a meaningful range
(stringent → very_permissive) for the speciation cluster
algorithm. The kernel's `speciation` module is exactly the
right machinery for testing this hypothesis. The clean PASS
provides a regression baseline for future kernel changes.

## API references in code chunks

4 field accesses (`base$mutation_sd`, `base$speciation`,
`base$speciation_cluster_interval`, plus one default). 0 stale
references.

## Deferred fixes (none required)

- This vignette is the methodology template for "Clean ✅
  reproductions" (referenced as such in paper-ryan-1990:187).
  Don't touch unless something specific changes.
