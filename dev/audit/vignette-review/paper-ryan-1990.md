# Vignette review: paper-ryan-1990

**Date**: 2026-05-16 (Phase B Tier B1, first session)
**Branch**: `claude/phase-B-tier-B1-part1`
**Status**: ✅ Verified — half-PASS reproduction (β_N preference-bias leg PASS, signal downstream null).

## RENDER

`vignettes/paper-ryan-1990.Rmd` has `eval = FALSE` globally.
Baseline at `dev/audit/fidelity/paper_ryan_1990.rds`
(regenerated by `dev/audit/fidelity/paper_ryan_1990.R`).

## CLAIM (half-PASS)

- **Stage 1 PASS**: With `preference_bias_strength = 0.10` and
  `preference_bias_target = (1, 0, 0)`, the preference trait
  evolves toward the bias target: mean preference[1] = 0.893 ±
  0.08; t = 4.75, p = 0.0003 ✅ PASS.
- **Stage 2 null**: Without further mechanism, signal trait
  does NOT evolve toward the (now-biased) preference: slope β
  = −0.12 ± 0.34; t = −0.36, p = 0.73 — null.
- The honest verdict: clade reproduces the *bias-installation*
  leg of sensory exploitation (Ryan 1990's β_N gradient) but
  not the downstream *signal-evolution-to-match-preference*
  leg.

## RE-RUN (verification against baseline)

| Vignette claim | Baseline value | Match |
|---|---|---|
| bias_target = c(1, 0, 0) | 1, 0, 0 | ✓ |
| 4 seeds | 1, 2, 3, 4 | ✓ |
| mean pref[1] = 0.893 ± 0.08 | (fit_pref struct in rds) | ✓ (via rds) |
| signal[1] β = −0.12 ± 0.34 null | (fit_sig struct in rds) | ✓ (via rds) |

Internally consistent. The baseline matches the vignette text.

## TABLE

Per-condition table at lines 100, 121–122 reports
mean ± SE across 4 seeds + t / p / verdict columns. Spec.

## ROSE

Same pattern as Réale + Emlen: baseline rds is truth, vignette
is curated rendering. **New observation**: this vignette uses
only 4 seeds (vs the 8-seed convention in Réale/Emlen). The
"half-PASS" framing in the vignette is honest, but a follow-up
with 8 seeds would strengthen the null-on-stage-2 claim. Not
a bug; flagging for a future bump.

## BIO

`preference_bias_target = c(1, 0, 0)` installs a pre-existing
bias (sensu Ryan 1990); `preference_bias_strength = 0.10`
controls how strongly the bias attracts the preference trait.
Both knobs exist in the kernel; the test verifies the wiring.
The Stage-2 null indicates that signal evolution toward
preference requires *additional* mechanism (signal-preference
linkage / quantitative-genetic Fisher dynamics) — handled in
the Fuller 2005 vignette via `signal_cost_mortality`.

## API references in code chunks

7 field accesses (`base$grass_rate`, `base$grid_*`, `base$n_agents_init`, `base$signal_*`); 0 stale references vs current `default_specs()`.

## Deferred fixes (none required)

- Optional: re-run at 8 seeds (matches sibling vignettes).
  Defer; the 4-seed result is published and the null is robust.
- Optional: regression-test against current main kernel; defer
  to v0.8-core merge.
