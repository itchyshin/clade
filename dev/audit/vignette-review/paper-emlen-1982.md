# Vignette review: paper-emlen-1982

**Date**: 2026-05-16 (Phase B Tier B1, first session)
**Branch**: `claude/phase-B-tier-B1-part1`
**Status**: ✅ Verified — vignette claims match the saved fidelity baseline; nuanced reproduction (per-capita PASS, genetic invasion FAIL).

## RENDER

`vignettes/paper-emlen-1982.Rmd` has `eval = FALSE` globally.
Baseline at `dev/audit/fidelity/paper_emlen_1982.rds`
(regenerated by `dev/audit/fidelity/paper_emlen_1982.R`).

## CLAIM (split verdict)

- **Headline (contradicts paper)**: Spearman ρ(grass_rate,
  helper_tendency) = **−0.115** (genetic helping does not
  evolve along the grass-availability gradient).
- **Accessory (reproduces paper)**: total_helper_events shows
  the predicted direction with strong magnitude — scarce vs
  abundant t = −13.0 (PASS), very_scarce vs abundant t = −18.1
  (PASS), moderate vs abundant t = −8.66 (PASS).
- The split: **per-capita helping rate reproduces Emlen's
  direction; genetic invasion of `helper_tendency` does not.**

## RE-RUN (verification against baseline)

| Vignette claim | Baseline value | Match |
|---|---|---|
| Spearman = −0.115 | −0.1150354 | ✓ |
| moderate t = −8.66 PASS | t = −8.6637 PASS | ✓ |
| scarce t = −13.0 PASS | t = −12.9641 PASS | ✓ (rounding) |
| very_scarce t = −18.1 PASS | t = −18.0993 PASS | ✓ |
| 4 conditions × 8 seeds | 8/8/8/8 = 32 runs | ✓ |

Internally consistent. No drift.

## TABLE

Per-condition table at line 130–132 already reports mean ± SE
across 8 seeds and the contrast t-statistic. Spec.

## ROSE

Same patterns confirmed (sibling to paper-reale-2010):

1. **Saved baseline rds is the source of truth.** Vignette is
   a curated rendering of the rds. Same as Réale.
2. **Vignette honestly reports the split verdict.** Total
   events reproduce, per-capita doesn't, genetic invasion
   doesn't. Each is explained on its own terms — the prose
   doesn't paper over the mixed result.

## BIO

The four grass_rate cells (abundant 0.20, moderate 0.10,
scarce 0.05, very_scarce 0.025) span a 8× resource gradient
— the right scale for testing Emlen's "ecological constraints
on helping" hypothesis. All conditions pass viability (visible
in the per-condition mean populations in the report tables).

The "genetic invasion does not occur" finding is biologically
honest: with `cooperative_breeding = TRUE` but no
`helper_kin_threshold` configuration, the model has the
mechanism but not the calibration for invasion. Vignette's
"Research path to clean ✅" section enumerates what would
need to change.

## API references in code chunks

Field accesses verified against current `default_specs()`. **0
stale references.**

## Deferred fixes (none required)

- Optional regression-test run on current main kernel; defer
  until v0.8-core merge.
