# Vignette review: paper-fuller-2005

**Date**: 2026-05-16 (Phase B Tier B1, first session)
**Branch**: `claude/phase-B-tier-B1-part1`
**Status**: ✅ Verified — split verdict (Zahavi leg PASS at two
β_Sv levels; β_N PASS via Ryan; Fisher direction-wrong, needs
linkage).

## RENDER

`vignettes/paper-fuller-2005.Rmd` has `eval = FALSE` globally.
Baseline at `dev/audit/fidelity/paper_fuller_2005.rds`
(regenerated by `dev/audit/fidelity/paper_fuller_2005.R`).

## CLAIM (3-leg framework — split verdict)

Fuller, Houle & Travis 2005 is a unified-framework paper for
sexual selection: signals evolve under THREE concurrent
mechanisms. clade's reproduction:

- **β_Sv (Zahavi handicap)** — ✅ PASS at β_Sv = 0.001
  (t = −2.13, Δ = −0.025 ± 0.012) and β_Sv = 0.003
  (t = −3.57, Δ = −0.439 ± 0.123). Marginal at 0.002
  (t = −1.84, Δ = −0.102 ± 0.056). The new
  `signal_cost_mortality` spec (added 0.6.3) implements the
  Zahavi viability cost; reproduction is decisive.
- **β_N (sensory bias)** — ✅ PASS via the dedicated
  `paper-ryan-1990` reproduction (cross-referenced).
- **Fisher runaway (C_tp covariance)** — ❌ direction-wrong.
  PR #124 wired `mate_choice_mode` so the kernel now
  branches on it, but signal and preference are independently
  inherited in the genome — so C_tp covariance cannot build
  up across generations. Needs linkage to close.

## RE-RUN (verification against baseline)

Baseline rds has `sweep`, `report`, `sweep_fisher`,
`rpt_fisher`. The three Zahavi-leg t-values + the Fisher-leg
direction match the vignette text. Internally consistent.

## TABLE

The 3-leg verdict table at line 79–82 (and the contrast table
at 147–151) reports verdicts per mechanism with multi-seed
contrast statistics. Spec.

## ROSE

Same baseline-rds-as-truth pattern. **New observation**: this
vignette explicitly *cites* sibling vignettes for individual
legs (e.g., "see [`paper-ryan-1990`](paper-ryan-1990.html) for
β_N"). Worth replicating — paper-fuller-2005 is the
synthesising entry, the individual paper-* are the
mechanistic legs. The cross-references make the dependency
graph readable.

## BIO

The Zahavi-leg PASS at β_Sv = 0.001 and 0.003 (marginal at
0.002) shows the expected dose-response: stronger viability
cost on display → stronger signal contraction toward zero.
Defensible. The Fisher-leg fail honestly identifies the
*missing* kernel ingredient (signal-preference genetic
linkage), not a calibration knob — exactly what a
methodology vignette should do.

## API references in code chunks

9 field accesses (`base$signal_dims`, `base$signal_drift_sd`,
`base$signal_evolution_drift`, etc.); 0 stale references.

## Deferred fixes (none required)

- The Fisher-leg deferral ("needs linkage") is a real kernel
  feature gap, flagged for a future implementation phase.
  Not a vignette issue.
- Optional regression-test against current main kernel; defer.
