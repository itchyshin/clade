% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{grid_specs}
\alias{grid_specs}
\title{Generate a factorial grid of specs}
\usage{
grid_specs(base, ..., seed_from = 1L)
}
\arguments{
\item{base}{A specs list (from \code{\link[=default_specs]{default_specs()}}, \code{\link[=fast_specs]{fast_specs()}}, etc.)
to use as the template for each combination.}

\item{...}{Named vectors / lists of candidate values for each parameter.
For example, \verb{grass_rate = c(0.1, 0.2, 0.3), mutation_sd = c(0.02, 0.05)}
generates a 3 × 2 = 6-cell grid. String-typed parameters can also be
passed (e.g. \code{life_history = c("iteroparous", "semelparous")}).}

\item{seed_from}{Integer or \code{NULL}. If provided, overrides each cell's
\code{random_seed} with \verb{seed_from + 0L, seed_from + 1L, ...} so a grid run
is reproducible. Default \code{1L}.}
}
\value{
A named list of specs. Names encode the parameter values, e.g.
\code{"grass_rate=0.1;mutation_sd=0.02"}.
}
\description{
Produces a \code{list} of specs objects, one per combination of the supplied
parameter values. Every other field is inherited from \code{base}. Useful for
systematic parameter-space exploration with \code{\link[=batch_alife]{batch_alife()}}.
}
\examples{
\dontrun{
base <- fast_specs()
specs_list <- grid_specs(base,
                         grass_rate   = c(0.1, 0.2, 0.3),
                         mutation_sd  = c(0.02, 0.05))
results <- batch_alife(specs_list, n_cores = 6L)
}

}
\seealso{
\code{\link[=batch_alife]{batch_alife()}}, \code{\link[=sample_specs]{sample_specs()}}, \code{\link[=summarize_batch]{summarize_batch()}}
}
