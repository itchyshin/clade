% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{take_action}
\alias{take_action}
\title{Choose an action for an agent given its sensory input}
\usage{
take_action(env, i = 1L, input = NULL)
}
\arguments{
\item{env}{Environment list from \code{\link[=run_alife]{run_alife()}}.}

\item{i}{Integer; index into \code{env$agents} (1-based).}

\item{input}{Numeric vector. Sensory input (from \code{\link[=sense_env]{sense_env()}}). If \code{NULL},
\code{sense_env(env, i)} is called automatically.}
}
\value{
A named list:
\describe{
\item{\verb{$action}}{Integer action index (1-based).}
\item{\verb{$logits}}{Raw output values from the final brain layer.}
\item{\verb{$probs}}{Softmax probabilities over actions.}
\item{\verb{$action_names}}{Character vector of action labels.}
}
}
\description{
\code{take_action()} runs the agent's brain forward pass on its current sensory
input and returns the chosen action index (1-indexed). Action indices match
the Julia convention: 1 = left, 2 = up, 3 = right, 4 = down, 5 = eat,
6 = reproduce (where 5 and 6 may not be available for all brain types).
}
\details{
For BNN brains the mean weights (\code{mu}) are used (the agent acts on the mode
of its weight posterior). For other brain types, \code{W} weights are used.
}
\examples{
\dontrun{
env <- run_alife(default_specs())
res <- take_action(env, 1L)
res$action        # which action was chosen
res$probs         # probability distribution over actions
}

}
\seealso{
\code{\link[=sense_env]{sense_env()}}, \code{\link[=inspect_brain]{inspect_brain()}}
}
