% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis.R
\name{hypothesis_report}
\alias{hypothesis_report}
\title{Compute contrast tests from a hypothesis_sweep}
\usage{
hypothesis_report(sweep, contrasts, metric = NULL)
}
\arguments{
\item{sweep}{A \code{hypothesis_sweep} object from \code{\link[=hypothesis_sweep]{hypothesis_sweep()}}.}

\item{contrasts}{Named list of pairwise contrasts. Each element is a
length-two character vector \code{c(reference, test)} naming
conditions present in \code{sweep$conditions}.}

\item{metric}{Character. Name of the metric column in \code{sweep$runs}
to test. Defaults to the first metric.}
}
\value{
An S3 \code{"hypothesis_report"} object — a list with \code{table}
(a data frame of contrast statistics) and \code{metric} (the metric
name). Has a \code{print()} method that renders the table.
}
\description{
\code{hypothesis_report()} produces t-tests (Welch, two-sample) for
named pairwise contrasts drawn from a \code{\link[=hypothesis_sweep]{hypothesis_sweep()}} result.
The conventional behavioural-ecology audit pattern is to name a
contrast with a direction-of-effect question ("does parental care
reduce population variance?") and report \verb{Δ ± SE} and \code{t} across
seeds.
}
\details{
Each contrast is a length-two character vector specifying
\code{c(reference_condition, test_condition)}. The reported delta is
\verb{mean(test) − mean(reference)} on the chosen metric; the
interpretation of the sign is left to the caller.

The verdict column uses the 2σ threshold convention shared across
clade's fidelity audits: \verb{|t| >= 2} → \strong{PASS}, \verb{1.5 <= |t| < 2} →
\strong{marginal}, otherwise → \strong{null}. This is a screening heuristic,
not a formal hypothesis test — report the underlying statistic in
publications.
}
\examples{
\dontrun{
sweep <- hypothesis_sweep(...)
hypothesis_report(sweep,
                  contrasts = list(
                    food_effect = c("low", "high"),
                    cost_under_stress = c("cost0_stress", "cost1_stress")
                  ),
                  metric = "final_n")
}

}
\seealso{
\code{\link[=hypothesis_sweep]{hypothesis_sweep()}}
}
