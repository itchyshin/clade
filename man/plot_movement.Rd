% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_movement}
\alias{plot_movement}
\title{Plot agent trajectories from a movement log}
\usage{
plot_movement(
  md,
  colour_by = c("energy", "age", "id", "alive"),
  tick = NULL,
  grid_rows = NULL,
  grid_cols = NULL,
  ...
)
}
\arguments{
\item{md}{A data frame returned by \code{\link[=get_movement_data]{get_movement_data()}}, or \code{NULL}
when \code{specs$log_movement = FALSE} was set for the run.}

\item{colour_by}{Character. Column of \code{md} used to colour points.
One of \code{"energy"} (default), \code{"age"}, \code{"id"}, or \code{"alive"}.}

\item{tick}{Optional integer. When supplied, only rows with \code{md$t == tick} are drawn; when \code{NULL} (default), all recorded ticks are
drawn together.}

\item{grid_rows, grid_cols}{Optional integers giving the grid
extent for \code{coord_fixed()}. When \code{NULL} (default), the extent is
inferred from \code{max(md$x)} and \code{max(md$y)}.}

\item{...}{Currently unused. Reserved for forward compatibility.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Renders the per-agent, per-tick positions logged when
\code{specs$log_movement = TRUE} was set for the run. With \code{tick = NULL}
(default) every recorded position is drawn as a point, showing the
cumulative trajectory of every agent over the run. With \code{tick = t}
only positions recorded at that tick are drawn, giving a single
snapshot equivalent to a still frame from \code{\link[=plot_run_movie]{plot_run_movie()}}.

When the movement log is \code{NULL} (recording was off), a placeholder
ggplot with an explanatory message is returned rather than an error.
}
\details{
Plot agent trajectories from a movement log
}
\examples{
\dontrun{
specs <- default_specs()
specs$log_movement      <- TRUE
specs$log_movement_freq <- 5L
env <- run_alife(specs)
md  <- get_movement_data(env)
plot_movement(md)                     # every recorded position
plot_movement(md, tick = 100L)        # single-tick snapshot
plot_movement(md, colour_by = "age")
}

}
\seealso{
\code{\link[=get_movement_data]{get_movement_data()}}, \code{\link[=plot_run_movie]{plot_run_movie()}}, \code{\link[=plot_map]{plot_map()}}
}
