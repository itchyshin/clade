% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis.R
\name{hypothesis_sweep}
\alias{hypothesis_sweep}
\title{Sweep hypothesis conditions across seeds and compute per-run metrics}
\usage{
hypothesis_sweep(
  base_specs,
  conditions,
  seeds = 1:8,
  metrics = NULL,
  n_cores = 1L,
  verbose = FALSE
)
}
\arguments{
\item{base_specs}{A specs list from \code{\link[=default_specs]{default_specs()}},
\code{\link[=fast_specs]{fast_specs()}}, \code{\link[=realistic_specs]{realistic_specs()}}, etc. Supplies the template
from which each run's spec is derived.}

\item{conditions}{Named list of conditions. Each element is itself
a named list of parameter overrides, e.g.
\code{list(cost_low = list(signal_cost = 0.0, grass_rate = 0.2))}.
The names appear in the output as the \code{condition} column.}

\item{seeds}{Integer vector of random seeds (default \code{1:8}). Each
condition is replicated across all seeds.}

\item{metrics}{Named list of functions. Each function takes the
\code{ticks} tibble returned by \code{\link[=get_run_data]{get_run_data()}} and returns a
length-one scalar (numeric or logical). Defaults collect
\code{final_n} (mean n_agents over the last 500 ticks) and \code{crashed}
(a logical — whether n_agents fell below 10 at run end).}

\item{n_cores}{Integer. Passed to \code{\link[=batch_alife]{batch_alife()}}. Set as close as
possible to \code{length(conditions) * length(seeds)} to keep each
worker on one run (subject to your machine's compute limits —
see CLAUDE.md for per-machine caps).}

\item{verbose}{Logical. Passed to \code{\link[=batch_alife]{batch_alife()}} (default FALSE).}
}
\value{
An S3 object of class \code{"hypothesis_sweep"} — a list with:
\itemize{
\item \code{runs}: tibble with one row per run, columns \code{condition},
\code{seed}, plus one column per metric.
\item \code{conditions}: the input conditions list (for re-referencing).
\item \code{metrics}: the input metrics list.
\item \code{base_specs}: the input base specs.
\item \code{seeds}: the seeds vector.
\item \code{elapsed}: difftime for the batch run.
}

The object has a \code{print()} method that summarises per-condition
means ± SE across seeds.
}
\description{
\code{hypothesis_sweep()} is the researcher-facing wrapper for the
common simulation workflow surfaced repeatedly in clade's fidelity
audits: define a base spec, vary a small number of parameters
across several conditions, replicate each condition across seeds,
and collect summary metrics from each run's tick log.
}
\details{
Each condition is a named list of parameter overrides applied on
top of \code{base_specs}. Conditions are crossed with \code{seeds} to produce
\code{length(conditions) * length(seeds)} runs, dispatched via
\code{\link[=batch_alife]{batch_alife()}}.

Metrics are evaluated on each run's \code{get_run_data(env)$ticks}
tibble (see \code{\link[=get_run_data]{get_run_data()}}) and must return a scalar or a
length-one logical. Common choices — mean over the last N ticks,
peak value, end-of-run value — are illustrated in the examples.
}
\examples{
\dontrun{
# Example: how does grass_rate affect equilibrium population?
specs <- fast_specs()
specs$max_ticks <- 1000L

sweep <- hypothesis_sweep(
  base_specs = specs,
  conditions = list(
    low  = list(grass_rate = 0.05),
    mid  = list(grass_rate = 0.10),
    high = list(grass_rate = 0.20)
  ),
  seeds = 1:8,
  n_cores = 24L
)
print(sweep)  # per-condition summary table

# Test a specific direction:
hypothesis_report(sweep,
                  contrasts = list(food_effect = c("low", "high")),
                  metric = "final_n")
}

}
\seealso{
\code{\link[=hypothesis_report]{hypothesis_report()}}, \code{\link[=batch_alife]{batch_alife()}}, \code{\link[=grid_specs]{grid_specs()}},
\code{\link[=default_specs]{default_specs()}}
}
