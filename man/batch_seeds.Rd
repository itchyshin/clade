% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{batch_seeds}
\alias{batch_seeds}
\title{Run one specs object with multiple random seeds}
\usage{
batch_seeds(specs, seeds = 1:5, n_cores = 1L, verbose = FALSE)
}
\arguments{
\item{specs}{A specs list from \code{\link[=default_specs]{default_specs()}} (or \code{\link[=quick_specs]{quick_specs()}} /
\code{\link[=full_specs]{full_specs()}}) with your modifications. The \code{random_seed} field is
overwritten for each replicate.}

\item{seeds}{Integer vector of seeds to use (default \code{1:5}).}

\item{n_cores}{Integer. Number of parallel R workers (default \code{1L}).
Passed to \code{\link[=batch_alife]{batch_alife()}}.}

\item{verbose}{Logical. Print progress (default \code{FALSE}).}
}
\value{
A named list of \code{env} objects, one per seed, named
\code{"seed_1"}, \code{"seed_2"}, etc.
}
\description{
\code{batch_seeds()} is a convenience wrapper around \code{\link[=batch_alife]{batch_alife()}} for
the common case of replicating a single simulation across several random
seeds. Each replicate is identical except for its \code{random_seed}.
}
\examples{
\dontrun{
s <- default_specs()
s$max_ticks <- 300L
results <- batch_seeds(s, seeds = 1:3)
lapply(results, function(e) tail(get_run_data(e)$ticks$mean_energy, 1))
}

}
\seealso{
\code{\link[=batch_alife]{batch_alife()}}, \code{\link[=default_specs]{default_specs()}}, \code{\link[=quick_specs]{quick_specs()}}
}
