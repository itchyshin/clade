% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_alife}
\alias{run_alife}
\title{Run an evolutionary simulation}
\usage{
run_alife(specs = default_specs(), verbose = TRUE)
}
\arguments{
\item{specs}{A named list of simulation parameters, typically from
\code{\link[=default_specs]{default_specs()}} with modifications. All parameters are documented in
\code{\link[=default_specs]{default_specs()}}.}

\item{verbose}{Logical. Print progress messages (default \code{TRUE}). Pass
\code{FALSE} for batch runs or testing.}
}
\value{
An \code{env} list with components:
\describe{
\item{\verb{$agents}}{A list of agent lists, one per surviving agent.}
\item{\verb{$t}}{Final tick number (equals \code{specs$max_ticks}).}
\item{\verb{$specs}}{The specs list used for this run (may differ from input
if \code{world_evolution = TRUE}).}
\item{\verb{$progress}}{A data frame of per-tick logged statistics (same as
\code{get_run_data(env)$ticks}).}
\item{\verb{$deaths}}{A data frame of per-death records (same as
\code{get_run_data(env)$deaths}).}
\item{\verb{$genome_log}}{A list of per-tick genome matrices (non-NULL only
when \code{specs$log_genomes = TRUE}).}
}
}
\description{
\code{run_alife()} is the primary entry point for clade. It sends \code{specs} to
Julia once, runs all \code{specs$max_ticks} ticks entirely in Julia, and returns
an environment list containing the final agent population and all logged
statistics. The Julia session is started automatically on the first call.
}
\details{
The R-Julia boundary is crossed \strong{once per call} regardless of simulation
length or agent count. This contrasts with Rcpp-based simulators (including
alifeR) where data is marshalled across the R-C++ boundary on every tick.
}
\examples{
\dontrun{
specs <- default_specs()
env   <- run_alife(specs)
data  <- get_run_data(env)
plot_run(data)

# Diploid run
specs$ploidy <- 2L
env2 <- run_alife(specs)

# BNN with epigenetics
specs$brain_type  <- "bnn"
specs$epigenetics <- TRUE
env3 <- run_alife(specs)
}

}
\seealso{
\code{\link[=default_specs]{default_specs()}}, \code{\link[=get_run_data]{get_run_data()}}, \code{\link[=batch_alife]{batch_alife()}},
\code{\link[=search_map_elites]{search_map_elites()}}
}
