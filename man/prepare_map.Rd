% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{prepare_map}
\alias{prepare_map}
\title{Validate and resize a habitat map}
\usage{
prepare_map(map, specs)
}
\arguments{
\item{map}{Integer or numeric matrix; a raw wall matrix.}

\item{specs}{Named list; simulation parameters from \code{\link[=default_specs]{default_specs()}}. Used
for dimension checking (\verb{$grid_rows}, \verb{$grid_cols}).}
}
\value{
An integer matrix of dimensions \code{specs$grid_rows} x
\code{specs$grid_cols} with values 0 or 1.
}
\description{
Checks that a wall matrix has the correct dimensions for a given specs list.
If dimensions differ, the map is rescaled via nearest-neighbour resampling
and a warning is issued. Values are coerced to 0/1 integer.
}
\examples{
specs   <- default_specs()
specs$grid_rows <- 20L; specs$grid_cols <- 20L
raw_map <- matrix(0L, nrow = 20L, ncol = 20L)
raw_map[1L, ] <- 1L; raw_map[20L, ] <- 1L
map <- prepare_map(raw_map, specs)
sum(map)   # number of wall cells
}
\seealso{
\code{\link[=generate_map]{generate_map()}}, \code{\link[=load_map]{load_map()}}
}
