% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{genome_distance}
\alias{genome_distance}
\title{Compute normalised Euclidean genome distance between two agents}
\usage{
genome_distance(agent_a, agent_b)
}
\arguments{
\item{agent_a, agent_b}{Agent lists from \code{env$agents}. Each must have a
\verb{$brain$layers} component.}
}
\value{
A non-negative numeric scalar. Zero means the two brains are
identical; values near 1 indicate substantial divergence.
}
\description{
\code{genome_distance()} quantifies how different two agents are by computing the
normalised Euclidean distance between their flattened brain weight vectors.
For BNN brains, the \code{mu} (mean) weights are used; for all other brain types,
the \code{W} weight matrices are used.
}
\examples{
\dontrun{
env <- run_alife(default_specs())
if (length(env$agents) >= 2L)
  genome_distance(env$agents[[1]], env$agents[[2]])
}
}
