% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{get_genome_data}
\alias{get_genome_data}
\title{Extract per-tick genome data (allele frequencies, diversity, FST)}
\usage{
get_genome_data(env)
}
\arguments{
\item{env}{An environment list returned by \code{\link[=run_alife]{run_alife()}}.}
}
\value{
A list with components:
\describe{
\item{\verb{$genomes}}{A list of matrices (one per logged tick). Each matrix
has one row per agent and one column per genome position. \code{NULL} when
\code{specs$log_genomes = FALSE}.}
\item{\verb{$heterozygosity}}{Reserved field — currently returns
\code{numeric(0L)}. Future versions will compute mean per-locus
heterozygosity across logged ticks.}
\item{\verb{$fst}}{Reserved field — currently returns \code{numeric(0L)}.
Future versions will compute per-tick FST (Weir & Cockerham 1984)
between species when \code{speciation = TRUE}.}
}
}
\description{
Returns genome-level statistics logged when \code{specs$log_genomes = TRUE}.
These include per-tick allele frequency vectors, mean heterozygosity,
linkage disequilibrium, and (when \code{speciation = TRUE}) per-species FST.
}
\examples{
\dontrun{
specs <- default_specs()
specs$log_genomes <- TRUE
env  <- run_alife(specs)
gdat <- get_genome_data(env)
head(gdat$genomes)   # tidy data.frame: t, agent_id, trait_1..trait_N
}

}
\references{
Weir, B.S. & Cockerham, C.C. (1984) Estimating F-statistics for the
analysis of population structure. \emph{Evolution} 38(6):1358-1370.
}
\seealso{
\code{\link[=get_run_data]{get_run_data()}}, \code{\link[=run_alife]{run_alife()}}
}
