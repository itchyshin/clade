% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{inspect_brain}
\alias{inspect_brain}
\title{Inspect the brain structure of a single agent}
\usage{
inspect_brain(env, agent_id = 1L)
}
\arguments{
\item{env}{An environment list returned by \code{\link[=run_clade]{run_clade()}}.}

\item{agent_id}{Integer. The \verb{$id} field of the agent to inspect.
Defaults to \code{1L}.}
}
\value{
A named list with elements:
\describe{
\item{\verb{$brain_type}}{Character. Value of \code{env$specs$brain_type}.}
\item{\verb{$n_layers}}{Integer. Number of layers in \code{brain$layers}.}
\item{\verb{$layer_sizes}}{List of integer vectors \code{c(nrow, ncol)} for each
layer weight matrix.}
\item{\verb{$n_weights}}{Integer. Total number of weight values across all
layers.}
\item{\verb{$weight_mean}}{Numeric. Mean of all weights.}
\item{\verb{$weight_sd}}{Numeric. Standard deviation of all weights.}
\item{\verb{$weight_min}}{Numeric. Minimum weight value.}
\item{\verb{$weight_max}}{Numeric. Maximum weight value.}
\item{\verb{$sigma_mean}, \verb{$sigma_sd}, \verb{$sigma_min}, \verb{$sigma_max}}{Numeric.
Statistics for the \verb{$sigma} matrices (BNN brains only).}
\item{\verb{$note}}{Character. Present only when \code{brain$layers} is
unavailable; explains why summary statistics are absent.}
}
}
\description{
\code{inspect_brain()} extracts structural and statistical information from the
brain of a named agent in \code{env$agents}. It supports both ANN brains
(layers with \verb{$W} and \verb{$b}) and BNN brains (layers with \verb{$mu} and
\verb{$sigma}). When the brain structure is unavailable it returns a minimal
list with a note.
}
\examples{
\dontrun{
env <- run_clade(default_specs())
inspect_brain(env, agent_id = 1L)
}

}
\seealso{
\code{\link[=get_brain_weights]{get_brain_weights()}}, \code{\link[=run_clade]{run_clade()}}
}
