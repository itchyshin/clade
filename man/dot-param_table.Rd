% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.param_table}
\alias{.param_table}
\title{Generate a parameter-reference table for one group (internal)}
\usage{
.param_table(group, specs = default_specs())
}
\arguments{
\item{group}{Character. Name of one of the groups in \code{.SPEC_GROUPS}.
See \code{\link[=.param_groups]{.param_groups()}} for the full list.}

\item{specs}{A specs list (default: \code{\link[=default_specs]{default_specs()}}).}
}
\value{
A \code{data.frame} with three columns: \code{parameter}, \code{default},
\code{type}. Empty \code{data.frame} if no field in \code{group} is in \code{specs}.
}
\description{
Builds a \code{data.frame} with columns \code{parameter}, \code{default}, \code{type} for
all fields in \code{specs} that belong to the named group. Used by
\code{vignette("parameter-reference")} so that defaults are always
synchronised with \code{default_specs()} rather than hand-maintained.
}
\details{
Fields listed in \code{.SPEC_GROUPS[[group]]} but absent from \code{specs} are
silently skipped (lets the groupings hold names of reserved-future
fields without breaking the table).
}
\keyword{internal}
