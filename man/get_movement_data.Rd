% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{get_movement_data}
\alias{get_movement_data}
\title{Extract per-tick agent-position log as a tidy data frame}
\usage{
get_movement_data(env)
}
\arguments{
\item{env}{An environment list returned by \code{\link[=run_alife]{run_alife()}}.}
}
\value{
A data.frame with columns \code{t} (integer), \code{id} (integer),
\code{x} (integer), \code{y} (integer), \code{age} (integer), \code{energy} (double),
\code{alive} (logical) - one row per (logged tick x agent). Returns
\code{NULL} when \code{log_movement = FALSE} for the run. A zero-row
data.frame (with the correct columns) is returned when logging
was enabled but no ticks were recorded (short runs at low
\code{log_movement_freq}).
}
\description{
\code{get_movement_data()} converts \code{env$movement_log} (opt-in per-tick
agent positions logged Julia-side when \code{specs$log_movement = TRUE})
into a single long data.frame with one row per (logged tick x agent).
}
\details{
Movement logging is off by default and off in the returned env when
the run did not enable it (\code{env$movement_log} is \code{NULL}), so
\code{get_movement_data()} returns \code{NULL} in that case. Downstream code
can guard cleanly with \code{if (is.null(md)) ...}.

To enable, set two extra specs before calling \code{\link[=run_alife]{run_alife()}}:
\preformatted{
  specs$log_movement      <- TRUE
  specs$log_movement_freq <- 5L   # record every 5 ticks; 1L = every tick
}

Neither field is in \code{\link[=default_specs]{default_specs()}} (trajectory logging is
memory-heavy and off by default); they are read on the Julia side
via \code{get(specs, "log_movement", false)} so extra keys pass through
the R-to-Julia bridge without modification.
}
\examples{
\dontrun{
specs <- default_specs()
specs$log_movement      <- TRUE
specs$log_movement_freq <- 5L
env <- run_alife(specs)
md  <- get_movement_data(env)
head(md)
plot_movement(md)
}

}
\seealso{
\code{\link[=get_run_data]{get_run_data()}}, \code{\link[=plot_movement]{plot_movement()}}, \code{\link[=plot_run_movie]{plot_run_movie()}},
\code{\link[=run_alife]{run_alife()}}
}
