% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{heritability_estimate}
\alias{heritability_estimate}
\title{Estimate narrow-sense heritability from parent-offspring data}
\usage{
heritability_estimate(data, trait = "num_offspring")
}
\arguments{
\item{data}{A list from \code{\link[=get_run_data]{get_run_data()}}, specifically using \code{data$deaths}.}

\item{trait}{Character; column name in \code{data$deaths} to use as the trait.
Default \code{"num_offspring"}.}
}
\value{
A list with:
\describe{
\item{\verb{$h2}}{Estimated heritability (slope of parent-offspring regression).
\code{NA} when fewer than 5 matched pairs are found.}
\item{\verb{$n_pairs}}{Number of matched parent-offspring pairs.}
\item{\verb{$method}}{\code{"parent_offspring_regression"}.}
\item{\verb{$trait}}{The trait used.}
}
}
\description{
\code{heritability_estimate()} computes h2 by regressing offspring trait values
on parent trait values using the \verb{$deaths} data frame returned by
\code{\link[=get_run_data]{get_run_data()}}. This is the parent-offspring regression method, as
distinct from the lag-1 autocorrelation approach used by
\code{\link[=estimate_heritability]{estimate_heritability()}}.
}
\details{
The deaths data frame must contain \code{parent_id} and the requested \code{trait}
column. Run the simulation long enough that agents die and are recorded.
}
\examples{
\dontrun{
env  <- run_alife(default_specs())
data <- get_run_data(env)
h    <- heritability_estimate(data, trait = "num_offspring")
h$h2
}
}
\seealso{
\code{\link[=estimate_heritability]{estimate_heritability()}} for the lag-1 autocorrelation approach.
}
