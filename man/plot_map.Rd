% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_map}
\alias{plot_map}
\title{Plot the spatial distribution of agents on the grid}
\usage{
plot_map(env, colour_by = "energy", ...)
}
\arguments{
\item{env}{An environment list returned by \code{\link[=run_clade]{run_clade()}}. Must contain
\verb{$specs} (with \code{grid_rows}, \code{grid_cols}, and optionally \code{grass_max}),
\verb{$grass} (numeric matrix), and \verb{$agents} (list of per-agent records).}

\item{colour_by}{Character scalar selecting the agent attribute used to
colour points. One of \code{"energy"} (continuous, blue scale), \code{"age"}
(continuous, orange scale), \code{"species"} (\code{agent$species_id}, discrete,
up to 10 colours), or \code{"body_size"} (continuous, purple scale).
Default: \code{"energy"}.}

\item{...}{Currently unused. Reserved for forward compatibility.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Renders a snapshot of the grid world with grass density as a green tile
heatmap and agent positions as coloured points. The colour of each agent
can represent energy, age, species identity, or body size, making it easy
to examine the spatial structure of the population at any tick.
}
\details{
Plot the spatial distribution of agents on the grid
}
\examples{
\dontrun{
env <- run_clade(default_specs())
plot_map(env)
plot_map(env, colour_by = "age")
}

}
\seealso{
\code{\link[=plot_environment]{plot_environment()}}, \code{\link[=plot_run]{plot_run()}}, \code{\link[=run_clade]{run_clade()}}
}
