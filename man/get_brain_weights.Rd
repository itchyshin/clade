% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{get_brain_weights}
\alias{get_brain_weights}
\title{Extract weight values from an agent's brain}
\usage{
get_brain_weights(env, agent_id = 1L, layer = NULL)
}
\arguments{
\item{env}{An environment list returned by \code{\link[=run_clade]{run_clade()}}.}

\item{agent_id}{Integer. The \verb{$id} field of the agent. Defaults to \code{1L}.}

\item{layer}{Integer or NULL. If \code{NULL} (default), all weights are returned
as a named numeric vector. If an integer, the weight matrix for that layer
index is returned.}
}
\value{
A named numeric vector (when \code{layer = NULL}) or a numeric matrix
(when \code{layer} is specified).
}
\description{
\code{get_brain_weights()} returns either all weights concatenated across layers
(when \code{layer = NULL}) or the weight matrix for a single specified layer.
For ANN brains the weight matrix is \verb{$W}; for BNN brains it is \verb{$mu}.
}
\examples{
\dontrun{
env <- run_clade(default_specs())
get_brain_weights(env, agent_id = 1L)           # all weights
get_brain_weights(env, agent_id = 1L, layer = 1L) # layer-1 matrix
}

}
\seealso{
\code{\link[=inspect_brain]{inspect_brain()}}, \code{\link[=run_clade]{run_clade()}}
}
