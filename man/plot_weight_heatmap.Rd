% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_weight_heatmap}
\alias{plot_weight_heatmap}
\title{Visualise a neural genome as a weight heatmap}
\usage{
plot_weight_heatmap(ann, title = "Neural genome")
}
\arguments{
\item{ann}{A brain list with a \verb{$layers} element, as returned by
\code{env$agents[[i]]$brain} for ANN brain types. Must be an R list (not a
JuliaConnectoR proxy).}

\item{title}{Character scalar prepended to each panel title.
Default: \code{"Neural genome"}.}
}
\value{
A \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}} object with one panel per layer.
}
\description{
Renders each weight matrix in an ANN-format brain (a list with a \code{layers}
element, each layer having \verb{$W} and \verb{$b}) as a diverging blue-white-red
tile heatmap. Blue = strong inhibitory connections, red = strong excitatory
connections, white = near-zero weights. One panel per layer.

Agents under strong selection often show structured weight matrices (certain
input-to-hidden connections consistently strong) compared to the near-random
patterns of newly initialised founders.
}
\details{
Visualise a neural genome as a weight heatmap
}
\examples{
\dontrun{
env <- run_clade(default_specs())
# Access brain data (requires converting from Julia proxy first)
# plot_weight_heatmap(brain_list)
}

}
\seealso{
\code{\link[=visualize_progress]{visualize_progress()}}
}
