% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_map_elites}
\alias{search_map_elites}
\title{MAP-Elites quality-diversity search over simulation parameters}
\usage{
search_map_elites(
  specs_base,
  archive_dims,
  n_iterations = 1000L,
  objective = "genetic_diversity",
  mutation_params = NULL,
  mutation_sd = 0.1,
  verbose = TRUE,
  checkpoint_path = NULL,
  checkpoint_every = 100L
)
}
\arguments{
\item{specs_base}{A specs list from \code{\link[=default_specs]{default_specs()}}. Used as the starting
point for mutations.}

\item{archive_dims}{A named list whose names are column names of
\code{get_run_data()$ticks} and whose values are numeric vectors specifying
the bin breakpoints for that dimension. Example:
\code{list(genetic_diversity = seq(0, 1, by = 0.1), n_species = 1:10)}.}

\item{n_iterations}{Integer. Number of MAP-Elites iterations (default 1000L).}

\item{objective}{Character or function. If a character, the name of a
column in \code{get_run_data()$ticks} to maximise (e.g. \code{"genetic_diversity"}).
If a function, must accept an \code{env} list and return a numeric scalar.}

\item{mutation_params}{Character vector of parameter names (from
\code{\link[=default_specs]{default_specs()}}) to mutate. Defaults to all numeric parameters.}

\item{mutation_sd}{Numeric. Standard deviation of Gaussian perturbations to
log-transformed parameter values (default 0.1).}

\item{verbose}{Logical. Print progress (default \code{TRUE}).}

\item{checkpoint_path}{Optional file path. If supplied, the current
archive + history + iteration index are saved to this RDS file every
\code{checkpoint_every} iterations (and once more at the end). If the
same path is passed to a subsequent call, the search resumes from
the saved iteration. Set to \code{NULL} (default) to disable
checkpointing. Added 0.5.6.}

\item{checkpoint_every}{Integer. How often to write the checkpoint,
in iterations (default 100L). Ignored when \code{checkpoint_path} is
\code{NULL}. Added 0.5.6.}
}
\value{
A list with components:
\describe{
\item{\verb{$archive}}{A list of lists, one per archive cell, each containing:
\verb{$specs}, \verb{$score}, \verb{$behavioural_descriptor} (named numeric vector).}
\item{\verb{$map}}{A ggplot2 heatmap of the archive scores (for 2D archive
dims only). \code{NULL} for higher-dimensional archives.}
\item{\verb{$history}}{Data frame with one row per iteration: \code{iteration},
\code{score}, \code{filled_cells}.}
}
}
\description{
Finds a diverse archive of high-performing parameter combinations using the
MAP-Elites algorithm (Mouret & Clune 2015). Rather than finding one optimal
parameter set, MAP-Elites maintains a grid of niches in behaviour space;
each cell holds the best-performing specs for that behavioural region.
}
\details{
\subsection{Algorithm}{
\enumerate{
\item Initialise: fill each archive cell by sampling random specs near
\code{specs_base}.
\item For each of \code{n_iterations} iterations:
a. Select a random filled cell from the archive.
b. Mutate its specs (Gaussian perturbation of numeric parameters).
c. Run \code{run_alife(new_specs)}.
d. Compute the behavioural descriptors from \code{get_run_data(env)}.
e. If the new specs score better than the current occupant of the
corresponding archive cell, replace it.
\item Return the full archive.
}
}
}
\examples{
\dontrun{
result <- search_map_elites(
  specs_base   = default_specs(),
  archive_dims = list(
    genetic_diversity = seq(0, 1, by = 0.1),
    n_species         = 1:10
  ),
  n_iterations = 500L,
  objective    = "genetic_diversity"
)
result$map   # ggplot2 heatmap
}

}
\references{
Mouret, J.-B. & Clune, J. (2015) Illuminating search spaces by mapping
elites. arXiv:1504.04909.
Chatzilygeroudis, K., Cully, A., Vassiliades, V. & Mouret, J.-B. (2021)
Quality-Diversity Optimization: a novel branch of stochastic optimization.
arXiv:2012.04322.
}
\seealso{
\code{\link[=search_cmaes]{search_cmaes()}}, \code{\link[=search_gradient]{search_gradient()}}, \code{\link[=run_alife]{run_alife()}}
}
