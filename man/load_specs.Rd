% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{load_specs}
\alias{load_specs}
\title{Load simulation specs from a JSON file}
\usage{
load_specs(path)
}
\arguments{
\item{path}{Character. Path to a JSON file. The file should contain a
single JSON object whose keys are parameter names from \code{\link[=default_specs]{default_specs()}}.
Numeric vectors of length 1 are read as scalars; longer vectors as
R vectors.}
}
\value{
A specs list (same format as \code{\link[=default_specs]{default_specs()}}).
}
\description{
Reads a JSON file containing simulation parameters and merges it over the
defaults from \code{\link[=default_specs]{default_specs()}}. Parameters not present in the JSON file
retain their defaults; parameters in the JSON that are not valid spec names
trigger a warning.
}
\details{
Load simulation specs from a JSON file
}
\examples{
\dontrun{
# specs.json contains {"mutation_sd": 0.3, "grass_rate": 0.6}
specs <- load_specs("path/to/specs.json")
env   <- run_alife(specs)
}

}
\seealso{
\code{\link[=default_specs]{default_specs()}}, \code{\link[=run_alife]{run_alife()}}
}
