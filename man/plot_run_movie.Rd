% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_run_movie}
\alias{plot_run_movie}
\title{Animate agent trajectories as a gganimate movie}
\usage{
plot_run_movie(
  md,
  colour_by = c("energy", "age", "id", "alive"),
  grid_rows = NULL,
  grid_cols = NULL,
  ...
)
}
\arguments{
\item{md}{A data frame returned by \code{\link[=get_movement_data]{get_movement_data()}}, or \code{NULL}.}

\item{colour_by}{Character. Passed to \code{\link[=plot_movement]{plot_movement()}}. Default
\code{"energy"}.}

\item{grid_rows, grid_cols}{Optional integers giving the grid
extent, passed to \code{\link[=plot_movement]{plot_movement()}}.}

\item{...}{Currently unused. Reserved for forward compatibility.}
}
\value{
A \code{gganim} object when \code{gganimate} is available and \code{md} is
non-\code{NULL}. A ggplot placeholder when \code{md} is \code{NULL}.
}
\description{
Builds a \code{gganim} object from a movement log for post-hoc playback.
Wraps \code{\link[=plot_movement]{plot_movement()}} with a \code{gganimate::transition_time()} layer
so successive frames show the population as it evolved. The
returned object is \strong{not rendered} — hand it to
\code{gganimate::animate()} (to view in RStudio) or
\code{gganimate::anim_save()} (to write a GIF or MP4).

Requires the \code{gganimate} package (in \code{Suggests}). When \code{gganimate}
is not installed the function errors with a clear install hint.
When \code{md} is \code{NULL} (recording was off) a placeholder ggplot is
returned rather than an error.
}
\details{
Animate agent trajectories as a gganimate movie
}
\examples{
\dontrun{
specs <- default_specs()
specs$log_movement      <- TRUE
specs$log_movement_freq <- 5L
env <- run_alife(specs)
md  <- get_movement_data(env)

mv <- plot_run_movie(md)
# Render options belong on animate() / anim_save():
gganimate::animate(mv, fps = 10, width = 480, height = 480)
gganimate::anim_save("run.gif", mv)
}

}
\seealso{
\code{\link[=plot_movement]{plot_movement()}}, \code{\link[=get_movement_data]{get_movement_data()}}
}
