% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modules.R
\name{register_module}
\alias{register_module}
\title{Register a custom module function}
\usage{
register_module(fn, when = "post_tick", name = NULL)
}
\arguments{
\item{fn}{A function with signature \verb{function(env) -> env}.}

\item{when}{Character; one of \code{"pre_tick"}, \code{"post_agents"},
\code{"post_tick"}, \code{"post_reproduce"}. Default \code{"post_tick"}.}

\item{name}{Character label for the module (optional; used in
\code{\link[=list_modules]{list_modules()}} output).}
}
\value{
Invisibly \code{NULL}. Side-effects: adds to the module registry.
}
\description{
Injects an R function into the clade simulation tick loop at the specified
hook point. The function receives a simplified environment snapshot
(\code{list(agents, grass, t, specs)}) and must return it (modified or
unmodified). Multiple modules may be registered at the same hook point;
they run in registration order.
}
\details{
Custom modules can modify:
\itemize{
\item \code{env$agents[[i]]$energy} — agent energy
\item \code{env$agents[[i]]$alive} — set to \code{FALSE} to kill an agent
\item \code{env$grass} — resource matrix (numeric matrix, rows x cols)
}
}
\examples{
# Kill any agent older than 100 ticks
register_module(
  fn   = function(env) {
    for (i in seq_along(env$agents)) {
      if (isTRUE(env$agents[[i]]$age > 100)) env$agents[[i]]$alive <- FALSE
    }
    env
  },
  when = "post_tick",
  name = "max_age_100"
)
clear_modules()
}
\seealso{
\code{\link[=list_modules]{list_modules()}}, \code{\link[=clear_modules]{clear_modules()}}, \code{\link[=run_alife]{run_alife()}}
}
