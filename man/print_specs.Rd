% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{print_specs}
\alias{print_specs}
\title{Pretty-print all simulation parameters}
\usage{
print_specs(specs = NULL, diff_only = FALSE)
}
\arguments{
\item{specs}{A named list of simulation parameters (from \code{\link[=default_specs]{default_specs()}}).
If \code{NULL} (default), prints the unmodified defaults.}

\item{diff_only}{Logical. If \code{TRUE}, only print parameters that differ from
\code{\link[=default_specs]{default_specs()}} defaults. Useful for inspecting a customised spec list.
Default \code{FALSE}.}
}
\value{
Invisibly, the \code{specs} list (for piping).
}
\description{
Prints every parameter in a \code{\link[=default_specs]{default_specs()}} list with its current value,
grouped by biological theme. Pass a modified specs list to see which
parameters differ from the defaults.
}
\examples{
print_specs()

s <- default_specs()
s$kin_selection <- TRUE
s$complex_landscape <- TRUE
print_specs(s, diff_only = TRUE)

}
