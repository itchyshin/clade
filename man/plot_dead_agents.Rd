% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_dead_agents}
\alias{plot_dead_agents}
\title{Plot lifetime statistics of dead agents}
\usage{
plot_dead_agents(run_data)
}
\arguments{
\item{run_data}{A list returned by \code{\link[=get_run_data]{get_run_data()}}. Must contain a
\verb{$deaths} data frame with columns \code{age}, \code{energy}, \code{cause},
\code{num_offspring}, and optionally \code{body_size}.}
}
\value{
A two-panel \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}} object, or \code{NULL} invisibly
if no agents died.
}
\description{
Produces two panels summarising agents that died during a run. The
\strong{left panel} plots age at death against energy at death, coloured by
body size (when body-size evolution is active) or number of offspring,
and sized by offspring count. The \strong{right panel} shows a bar chart of
cause-of-death counts (starvation, predation, disease, old age). Together
these reveal which life-history strategies were rewarded by selection.
}
\details{
Plot lifetime statistics of dead agents
}
\examples{
\dontrun{
env  <- run_clade(default_specs())
data <- get_run_data(env)
plot_dead_agents(data)
}

}
\seealso{
\code{\link[=get_run_data]{get_run_data()}}, \code{\link[=visualize_progress]{visualize_progress()}}
}
