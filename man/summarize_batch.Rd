% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{summarize_batch}
\alias{summarize_batch}
\title{Summarize a batch of run results into a tidy data frame}
\usage{
summarize_batch(results, specs_list, param_names = NULL, metrics = NULL)
}
\arguments{
\item{results}{A list of \code{env} objects from \code{\link[=batch_alife]{batch_alife()}}.}

\item{specs_list}{The list of specs passed to \code{batch_alife()}, so the
parameter values can be recovered. Must have the same length as
\code{results}.}

\item{param_names}{Character vector of spec field names to extract.
If \code{NULL} (default), infers them from the first spec by taking
every field that differs from \code{\link[=default_specs]{default_specs()}}.}

\item{metrics}{Named list of functions. Each function takes a single
\code{env} and returns a scalar numeric. Default metrics: final population
size, final mean energy, final genetic diversity, viability verdict.}
}
\value{
A data frame with one row per run. Columns: the named
parameters, each metric, and \code{viability} (the verdict string).
}
\description{
Pulls the parameter values from each spec and summary stats from the
corresponding run result into a single row, returning a data frame
suitable for plotting or filtering. Intended as the lightweight
companion to \code{\link[=batch_alife]{batch_alife()}} for parameter-space exploration.
}
\examples{
\dontrun{
specs_list <- sample_specs(fast_specs(), n = 100L,
                           grass_rate = list(0.05, 0.4))
results <- batch_alife(specs_list, n_cores = 10L)
tbl <- summarize_batch(results, specs_list,
                       param_names = "grass_rate")
plot(tbl$grass_rate, tbl$n_final)
}

}
\seealso{
\code{\link[=batch_alife]{batch_alife()}}, \code{\link[=grid_specs]{grid_specs()}}, \code{\link[=sample_specs]{sample_specs()}},
\code{\link[=viability_report]{viability_report()}}
}
