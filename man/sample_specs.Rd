% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{sample_specs}
\alias{sample_specs}
\title{Sample specs randomly from parameter distributions}
\usage{
sample_specs(base, n, ..., seed = 1L, seed_from = 1L)
}
\arguments{
\item{base}{A specs list template (see \code{\link[=grid_specs]{grid_specs()}}).}

\item{n}{Integer. Number of specs to draw.}

\item{...}{Named distributions. See Details.}

\item{seed}{Integer. Seed for the R-side sampler so the draw is
reproducible. Default \code{1L}.}

\item{seed_from}{Integer or \code{NULL}. Base for each drawn spec's
\code{random_seed}. Default \code{1L}.}
}
\value{
A named list of specs. Names are \code{"sample_1"}, \code{"sample_2"}, …
}
\description{
Draws \code{n} specs from a set of univariate distributions. Each distribution
is either a numeric vector (sampled with replacement), a function of one
argument \code{n} that returns \code{n} values, or a two-element list
\code{list(runif_min, runif_max)} for uniform random draws.
}
\details{
Three ways to specify a distribution for each parameter:
\itemize{
\item \strong{Vector}: \code{grass_rate = c(0.05, 0.1, 0.2, 0.3, 0.5)} — draws from
the vector with replacement.
\item \strong{Range (list of 2)}: \code{mutation_sd = list(0.01, 0.1)} — uniform
draw from [0.01, 0.1]. Useful when the parameter is continuous.
\item \strong{Function}: \code{plasticity_init_mean = function(n) rbeta(n, 2, 2)} —
any function that takes \code{n} and returns \code{n} values.
}
}
\examples{
\dontrun{
base <- fast_specs()
specs_list <- sample_specs(base, n = 500L,
                           grass_rate   = list(0.05, 0.40),
                           mutation_sd  = c(0.02, 0.05, 0.1),
                           plasticity_init_mean = function(n) rbeta(n, 2, 2))
results <- batch_alife(specs_list, n_cores = 50L)
summary_tbl <- summarize_batch(results, specs_list)
}

}
\seealso{
\code{\link[=batch_alife]{batch_alife()}}, \code{\link[=grid_specs]{grid_specs()}}, \code{\link[=summarize_batch]{summarize_batch()}}
}
