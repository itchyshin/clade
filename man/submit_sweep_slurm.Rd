% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{submit_sweep_slurm}
\alias{submit_sweep_slurm}
\title{Generate a SLURM array-job template for a parameter-space sweep}
\usage{
submit_sweep_slurm(
  specs_list,
  out_path,
  script_path,
  rds_path,
  n_array_tasks = min(100L, length(specs_list)),
  n_cores_per_task = 4L,
  time = "06:00:00",
  mem = "8G",
  summary_fn = NULL,
  R_library_path = NULL,
  extra_sbatch_lines = character(0)
)
}
\arguments{
\item{specs_list}{List of specs (e.g. from \code{\link[=sample_specs]{sample_specs()}}).}

\item{out_path}{Character. Path on the cluster filesystem where
the CSV will be appended to. Must be reachable from every node.}

\item{script_path}{Character. Local path where the \code{.sh} file is
written.}

\item{rds_path}{Character. Local path where the \code{.rds} of
\code{specs_list} is written. Cluster nodes need to be able to read
this path; for a shared filesystem just put it somewhere the
cluster can see.}

\item{n_array_tasks}{Integer. Number of SLURM array tasks. Each
gets \code{ceiling(length(specs_list) / n_array_tasks)} specs.
Default: \code{min(100, length(specs_list))}.}

\item{n_cores_per_task}{Integer. \code{n_cores} passed to
\code{\link[=stream_specs_to_csv]{stream_specs_to_csv()}} within each array task. Default 4L.}

\item{time}{Character. SLURM \code{--time} value (e.g. \code{"06:00:00"}).}

\item{mem}{Character. SLURM \code{--mem} value (e.g. \code{"8G"}).}

\item{summary_fn}{Optional summary function, same as
\code{\link[=stream_specs_to_csv]{stream_specs_to_csv()}}. If \code{NULL} (default), the default
summary is used on the cluster side.}

\item{R_library_path}{Optional character. Added to \code{.libPaths()}
on the cluster node via \code{.libPaths(c("<path>", .libPaths()))}
before \code{library(clade)}. Useful when clade is installed in a
non-standard location on the cluster.}

\item{extra_sbatch_lines}{Character vector of extra \verb{#SBATCH}
directives (without the leading \verb{#SBATCH}) to include in the
script preamble.}
}
\value{
The path to the generated shell script (invisibly), with
a message showing the \code{sbatch} command to invoke.
}
\description{
Writes two files that together let you fan a clade sweep across a
SLURM cluster: an \code{.rds} with the full specs_list, and a shell
script that calls \verb{Rscript -e 'clade::stream_specs_to_csv(...)'}
for a subset of specs per array task. You run the sweep yourself
with \verb{sbatch <script>.sh} — this function does not talk to SLURM.
}
\details{
Per-task behaviour: reads \code{SLURM_ARRAY_TASK_ID} from the
environment, selects the corresponding slice of the specs_list,
and appends summary rows to the shared \code{out_path} CSV. Because
\code{\link[=stream_specs_to_csv]{stream_specs_to_csv()}} is resume-safe, re-running any array task
is idempotent — useful if some jobs get preempted.
}
\examples{
\dontrun{
specs_list <- sample_specs(fast_specs(), n = 100000L,
                           grass_rate  = list(0.05, 0.45),
                           mutation_sd = c(0.05, 0.1, 0.2))
submit_sweep_slurm(
  specs_list,
  out_path     = "/shared/sweeps/big_sweep.csv",
  script_path  = "/shared/sweeps/submit.sh",
  rds_path     = "/shared/sweeps/specs.rds",
  n_array_tasks = 200L,
  n_cores_per_task = 8L,
  time         = "12:00:00",
  mem          = "16G")
# then: sbatch /shared/sweeps/submit.sh
}

}
\seealso{
\code{\link[=stream_specs_to_csv]{stream_specs_to_csv()}}, \code{\link[=sample_specs]{sample_specs()}}
}
