% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{stream_specs_to_csv}
\alias{stream_specs_to_csv}
\title{Stream a parameter-space sweep to disk, one row per run}
\usage{
stream_specs_to_csv(
  specs_list,
  out_path,
  summary_fn = NULL,
  n_cores = 1L,
  resume = TRUE,
  flush_every = 1L
)
}
\arguments{
\item{specs_list}{List of specs.}

\item{out_path}{Path to the CSV. Created if absent; appended if
present. First row is always a header.}

\item{summary_fn}{Function \verb{(env, specs) -> named list or one-row data.frame}. Should return the summary stats you want to save
per run. Default: viability verdict + n_final + mean_energy_final
\itemize{
\item genetic_diversity_final + the run's random_seed and any params
that differ from \code{\link[=default_specs]{default_specs()}}.
}}

\item{n_cores}{Integer. Parallel workers, as in \code{\link[=batch_alife]{batch_alife()}}.}

\item{resume}{Logical. If \code{TRUE} (default) and \code{out_path}
already exists, skip runs whose \code{run_id} is in the existing CSV.
Set \code{FALSE} to overwrite.}

\item{flush_every}{Integer. How often to flush the CSV to disk
(in rows). Default 1 (flush every row — safest; small I/O cost).}
}
\value{
The path to the CSV (invisibly). Read back with
\code{read.csv(out_path)}.
}
\description{
The counterpart to \code{\link[=batch_alife]{batch_alife()}} + \code{\link[=summarize_batch]{summarize_batch()}} for long
sweeps: runs each spec in \code{specs_list}, extracts a scalar summary
per run via \code{summary_fn}, and appends one CSV row to \code{out_path} as
each run completes. Two advantages over batching:
}
\details{
\itemize{
\item \strong{Memory}: the full \code{env} object for each run is discarded after
the summary row is written, so a 1 M-run sweep doesn't accumulate
1 M copies of \code{progress} / \code{deaths} in RAM.
\item \strong{Resumability}: if the job dies at run 800 000 of 1 000 000,
re-running with the same \code{out_path} picks up after the last
written row (matched by the \code{run_id} column).
}
}
\examples{
\dontrun{
specs_list <- sample_specs(fast_specs(), n = 10000L,
  grass_rate  = list(0.05, 0.40),
  mutation_sd = c(0.02, 0.05, 0.10))
stream_specs_to_csv(specs_list, "/tmp/sweep.csv", n_cores = 50L)
tbl <- read.csv("/tmp/sweep.csv")   # even if 10k × 50 B = 500 KB
}

}
\seealso{
\code{\link[=batch_alife]{batch_alife()}}, \code{\link[=sample_specs]{sample_specs()}}, \code{\link[=summarize_batch]{summarize_batch()}}
}
