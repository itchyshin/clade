% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_viability}
\alias{search_viability}
\title{Grid-search parameter viability: which combinations allow population survival?}
\usage{
search_viability(
  specs_base,
  param_x,
  values_x,
  param_y = NULL,
  values_y = NULL,
  n_reps = 3L,
  survival_threshold = 0.1,
  objective = NULL,
  verbose = TRUE
)
}
\arguments{
\item{specs_base}{A specs list from \code{\link[=default_specs]{default_specs()}}.}

\item{param_x}{Character. Name of the first parameter to vary.}

\item{values_x}{Numeric vector of values to test for \code{param_x}.}

\item{param_y}{Character or \code{NULL}. Optional second parameter (creates a 2D
grid when supplied).}

\item{values_y}{Numeric vector of values for \code{param_y}. Ignored if
\code{param_y = NULL}.}

\item{n_reps}{Integer. Replicates per grid cell (different random seeds;
default 3L).}

\item{survival_threshold}{Numeric. Fraction of initial agents that must
survive for a run to count as viable (default 0.1).}

\item{objective}{Character or function or \code{NULL}. If supplied, the mean
objective score across surviving replicates is added as column
\code{mean_objective} (default \code{NULL}).}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
A list with:
\describe{
\item{\verb{$data}}{Data frame with columns \code{param_x}, optionally \code{param_y},
\code{viability} (fraction surviving), \code{mean_final_pop}, and optionally
\code{mean_objective}.}
\item{\verb{$map}}{A ggplot2 tile/line plot coloured by viability.}
}
}
\description{
Runs a grid of parameter combinations and measures the fraction of replicates
in which the population survives to the end of the simulation. Returns both a
data frame and a ggplot2 heatmap so you can quickly identify the "viable
region" of parameter space before running CMA-ES or MAP-Elites inside it.
}
\details{
This directly answers the question "which parameters allow organisms to
evolve?" without requiring CMA-ES to converge first.
}
\examples{
\dontrun{
s <- default_specs()
s$complex_landscape <- TRUE
vm <- search_viability(
  s,
  param_x = "shrub_density",  values_x = seq(0.1, 0.5, 0.1),
  param_y = "canopy_density", values_y = seq(0.05, 0.3, 0.1),
  n_reps  = 3L
)
vm$map          # heatmap
vm$data         # raw viability fractions
}

}
\seealso{
\code{\link[=search_cmaes]{search_cmaes()}}, \code{\link[=tune_complex_landscape]{tune_complex_landscape()}}
}
