% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{generate_map}
\alias{generate_map}
\title{Generate a procedural habitat map}
\usage{
generate_map(
  type = "random_cluster",
  grid_rows = 50L,
  grid_cols = 50L,
  p_wall = 0.3,
  scale = 4,
  corridor_width = 2L,
  seed = NULL
)
}
\arguments{
\item{type}{Character; one of \code{"open"}, \code{"patchy"}, \code{"random_cluster"},
\code{"gaussian_field"}, \code{"corridors"}. Default \code{"random_cluster"}.}

\item{grid_rows}{Integer; number of grid rows. Default 50.}

\item{grid_cols}{Integer; number of grid columns. Default 50.}

\item{p_wall}{Numeric in (0, 1); target proportion of wall cells. Default
0.3. Ignored for \code{"open"}.}

\item{scale}{Positive numeric; spatial scale of wall clustering (sigma for
Gaussian, autocorrelation range for \code{nlm_gaussianfield}). Default 4.}

\item{corridor_width}{Integer; open-corridor width in cells (\code{"corridors"}
only). Default 2.}

\item{seed}{Integer or \code{NULL}; random seed. Default \code{NULL}.}
}
\value{
An integer matrix (0 = open, 1 = wall) of dimensions
\code{grid_rows} x \code{grid_cols}. Assign to \code{specs$wall_map} before calling
\code{\link[=run_alife]{run_alife()}}, or pass to \code{\link[=prepare_map]{prepare_map()}} first.
}
\description{
Creates a 0/1 wall matrix for use with \code{\link[=run_alife]{run_alife()}}. Five landscape types
are available, ranging from completely open to structured corridor networks.
Pass the result to \code{\link[=prepare_map]{prepare_map()}} to validate dimensions before use.
}
\details{
Types \code{"random_cluster"} and \code{"gaussian_field"} use the \code{NLMR} package
when installed; otherwise a pure-R Gaussian-smoothed fallback is used.
\code{NLMR} was removed from CRAN; install from
\href{https://github.com/ropensci/NLMR}{ropensci/NLMR} (e.g.
\code{remotes::install_github("ropensci/NLMR")}) if you want the higher-quality
neutral-landscape generators.
}
\examples{
map <- generate_map("random_cluster", grid_rows = 20L, grid_cols = 20L,
                    p_wall = 0.25, seed = 1L)
mean(map == 0L)  # fraction open
}
\seealso{
\code{\link[=load_map]{load_map()}}, \code{\link[=prepare_map]{prepare_map()}}
}
