% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{diversity_landscape}
\alias{diversity_landscape}
\title{Visualise the multi-trait diversity landscape}
\usage{
diversity_landscape(run_data, traits = NULL)
}
\arguments{
\item{run_data}{A list from \code{\link[=get_run_data]{get_run_data()}}. Must contain a \verb{$ticks}
data frame with a \code{t} column and at least one \verb{mean_*} trait column.}

\item{traits}{Character vector of \verb{mean_*} column names to plot.
\code{NULL} (default) auto-detects from the standard set of trait columns.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} faceted plot object.
}
\description{
Creates a faceted time-series showing the evolution of multiple heritable
trait means plus/minus one standard deviation across ticks. Each facet
corresponds to one trait. Only traits that are present in \code{run_data$ticks}
and have non-zero variance across the run are shown. When SD columns exist
(e.g. \code{sd_body_size} for \code{mean_body_size}) a shaded ribbon is drawn around
the mean trajectory.
}
\details{
Visualise the multi-trait diversity landscape
}
\examples{
\dontrun{
env  <- run_clade(default_specs())
data <- get_run_data(env)
diversity_landscape(data)
}

}
\seealso{
\code{\link[=plot_run]{plot_run()}}, \code{\link[=plot_module_metrics]{plot_module_metrics()}}
}
