% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{viability_report}
\alias{viability_report}
\title{Viability report for an evolutionary-audit run}
\usage{
viability_report(
  run_data,
  n_agents_init = NULL,
  crashed_frac = 0.2,
  weak_frac = 0.5,
  min_n = 20L
)
}
\arguments{
\item{run_data}{A list from \code{\link[=get_run_data]{get_run_data()}} — either a single
\verb{$ticks} data frame or the full \code{get_run_data()} output.}

\item{n_agents_init}{Integer. The initial agent count used to
seed the run. Required because \code{run_data} does not carry the
spec. Pass \code{NULL} (default) to use the first-tick \code{n_agents}
(which approximates init-mean after the first wave of births,
and is usually close enough).}

\item{crashed_frac}{Numeric in (0, 1). A run is declared
\code{"crashed"} if \code{n_final < crashed_frac * n_agents_init}.
Default 0.2 (final population less than 20\% of init).}

\item{weak_frac}{Numeric in (0, 1). A run is declared \code{"weak"}
(viable but with low confidence) if
\code{n_final < weak_frac * n_agents_init}. Default 0.5.}

\item{min_n}{Integer. Absolute minimum n_final below which the
run is \code{"crashed"} regardless of \code{crashed_frac}. Default 20
(at fewer than 20 agents, any trait mean is dominated by a
handful of individuals). Set to \code{0} to disable this floor.}
}
\value{
A list with:
\describe{
\item{\code{verdict}}{One of \code{"viable"}, \code{"weak"}, \code{"crashed"}.}
\item{\code{n_init}}{First-tick n_agents used as the reference.}
\item{\code{n_final}}{Last-tick n_agents.}
\item{\code{n_min}}{Minimum n_agents across the whole run.}
\item{\code{frac_final}}{\code{n_final / n_init}.}
\item{\code{frac_min}}{\code{n_min / n_init}.}
\item{\code{tick_of_min}}{First tick where \code{n_min} was reached.}
\item{\code{message}}{A one-line diagnostic suitable for logging.}
}
}
\description{
Checks whether a \code{run_alife()} result is viable enough to support
claims about \emph{evolved} trait values. Population crashes (agents
dying faster than they reproduce) silently corrupt trait-mean
audits by over-weighting a few lucky survivors. This function
quantifies crash risk via three metrics and returns a tidy report
together with a verdict in \verb{\{"viable", "weak", "crashed"\}}.
}
\details{
Motivation: during the 2026-04-17 fast_specs re-audit of
s-plasticity and s-dispersal-ifd, direction flips in the
5-seed multi-seed results were traced to seasonal runs where
\code{n_final < 20} while stable runs maintained healthy populations.
The trait-mean average over 0-5 surviving agents is dominated by
the specific crash trajectory rather than by any evolutionary
signal. This utility codifies the "check n_final before trusting
trait-mean effects" rule into a reusable check.
}
\examples{
\dontrun{
env  <- run_alife(fast_specs())
vr   <- viability_report(get_run_data(env))
print(vr)
# Guard audit claims on viability:
if (vr$verdict == "crashed") {
  warning("crash-driven result; trait means are unreliable")
}
}

Note: [hypothesis_sweep()]'s default `crashed` metric uses a
*different* threshold (absolute floor of 10 agents at run end)
for a *different* question. Use `viability_report()` to gate
interpretability (Hamilton-rule-style "is this trait mean
meaningful?"); use the sweep's `crashed` metric to count
per-condition extinctions in a sweep summary.

}
\seealso{
\code{\link[=run_alife]{run_alife()}}, \code{\link[=get_run_data]{get_run_data()}}, \code{\link[=hypothesis_sweep]{hypothesis_sweep()}}.
}
