% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{compare_conditions}
\alias{compare_conditions}
\title{Compare evolutionary outcomes across simulation conditions}
\usage{
compare_conditions(conditions, burn_in = 100L, plot = TRUE)
}
\arguments{
\item{conditions}{A named list of \code{run_data} objects, each from
\code{\link[=get_run_data]{get_run_data()}}. Names are used as condition labels.}

\item{burn_in}{Integer. Ticks to discard as burn-in (default \code{100L}).}

\item{plot}{Logical. If \code{TRUE}, returns a ggplot2 bar-chart comparison.
If \code{FALSE}, returns only the summary data frame.}
}
\value{
A data frame with one row per condition and columns \code{condition},
then mean and SD columns for each outcome metric (e.g. \code{genetic_diversity},
\code{genetic_diversity_sd}). If \code{plot = TRUE}, a \verb{$plot} attribute is also
attached.
}
\description{
Takes a named list of \code{run_data} objects (one per condition) and computes
post-burn-in means and standard deviations for key outcome metrics. Returns
a tidy data frame for downstream analysis or plotting.

Metrics included (when present in all runs):
\code{n_agents}, \code{genetic_diversity}, \code{mean_energy}, \code{n_births}, \code{n_deaths},
\code{grass_coverage}, \code{n_species}, \code{mean_body_size}, \code{mean_toxicity},
\code{mean_plasticity}, \code{n_predators}.
}
\details{
Compare evolutionary outcomes across simulation conditions
}
\examples{
\dontrun{
s1 <- default_specs(); s1$mutation_sd <- 0.05
s2 <- default_specs(); s2$mutation_sd <- 0.30
env1 <- run_alife(s1); env2 <- run_alife(s2)
result <- compare_conditions(list(low_mut = get_run_data(env1),
                                  hi_mut  = get_run_data(env2)))
result
}

}
\seealso{
\code{\link[=get_run_data]{get_run_data()}}, \code{\link[=estimate_heritability]{estimate_heritability()}}, \code{\link[=run_alife]{run_alife()}}
}
