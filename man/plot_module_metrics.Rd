% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_module_metrics}
\alias{plot_module_metrics}
\title{Plot module-specific metrics from a clade simulation run}
\usage{
plot_module_metrics(run_data)
}
\arguments{
\item{run_data}{A list as returned by \code{\link[=get_run_data]{get_run_data()}}. Must contain a
\verb{$ticks} data frame. The new module columns (\code{n_predators}, \code{n_helpers},
\code{mean_signal_magnitude}, \code{mean_toxicity}, \code{mean_plasticity},
\code{mean_helper_tendency}, \code{n_species}, \code{n_juveniles}, \code{n_toxic_attacks},
\code{n_avoided_attacks}) are used when present; absent columns are silently
skipped.}
}
\value{
A \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}} object with up to six panels arranged in
at most three columns, or a single \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} placeholder when
fewer than two panels are active.
}
\description{
Detects which optional simulation modules were active during a run by
inspecting the logged tick columns in \code{run_data$ticks}, then assembles up
to six panels into a patchwork grid. Only panels for modules that produced
non-zero data are included:

\describe{
\item{Predators}{\code{n_predators} — line plot of predator population over
time.}
\item{Species}{\code{n_species} — step plot of species count; shown only when
speciation was active (max > 1).}
\item{Traits}{Overlay of \code{mean_toxicity}, \code{mean_plasticity}, and
\code{mean_helper_tendency} as three coloured lines.}
\item{Signals}{\code{mean_signal_magnitude} — line plot; shown only when
signal evolution was active (max > 0).}
\item{Parental care}{\code{n_juveniles} — line plot of juveniles under care;
shown only when max > 0.}
\item{Mimicry}{\code{n_toxic_attacks} and \code{n_avoided_attacks} — two lines
showing attacks versus avoidance events; shown only when mimicry attacks
occurred (max n_toxic_attacks > 0).}
}

If fewer than two panels are active, a single informative placeholder plot
is returned instead.
}
\details{
Plot module-specific metrics from a clade simulation run
}
\examples{
\dontrun{
specs <- default_specs()
specs$n_predators_init  <- 10L
specs$mimicry           <- TRUE
env  <- run_clade(specs)
data <- get_run_data(env)
plot_module_metrics(data)
}

}
\seealso{
\code{\link[=plot_run]{plot_run()}}, \code{\link[=visualize_progress]{visualize_progress()}}, \code{\link[=get_run_data]{get_run_data()}}
}
