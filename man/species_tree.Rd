% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{species_tree}
\alias{species_tree}
\title{Reconstruct a species tree from a logged simulation}
\usage{
species_tree(run_data)
}
\arguments{
\item{run_data}{A list returned by \code{\link[=get_run_data]{get_run_data()}}.}
}
\value{
A list with components:
\describe{
\item{\verb{$tree}}{Always \code{NULL} in the current implementation.}
\item{\verb{$note}}{Character. Explains that species-tree reconstruction
awaits the speciation module.}
}
}
\description{
\code{species_tree()} is a placeholder. Phylogenetic reconstruction requires
the speciation module (Phase 2), which assigns agents to discrete
species, tracks lineage splits, and emits a per-tick species log. None
of that machinery is in place yet, so this function currently returns a
stub for forward compatibility.
}
\examples{
\dontrun{
env <- run_alife(default_specs())
species_tree(get_run_data(env))
}

}
\seealso{
\code{\link[=get_run_data]{get_run_data()}}, \code{\link[=estimate_heritability]{estimate_heritability()}}, \code{\link[=compute_ld]{compute_ld()}}
}
