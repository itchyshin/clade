% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{wolf_personality_specs}
\alias{wolf_personality_specs}
\title{Spec preset for the Wolf 2007 personality reproduction}
\usage{
wolf_personality_specs()
}
\value{
A specs list ready for \code{run_alife()}.
}
\description{
Returns a specs list configured to reproduce the boldness-aggressiveness
syndrome from Wolf, van Doorn, Leimar & Weissing (2007) Nature 447:581-584,
implemented in clade's spatially-explicit framework. See the vignette
\code{paper-wolf2007.Rmd} for the full reproduction context and discussion of
the spatially-explicit interpretation (vs Wolf's mean-field model).
}
\details{
Key parameter choices vs \code{default_specs()}:
\describe{
\item{\code{personality_syndrome = TRUE}}{Enable the module.}
\item{\code{min_repro_energy = 1e9}}{Disable clade's standard energy-triggered
reproduction so only Wolf age-windowed reproduction fires.}
\item{\code{min_repro_age = 0L}}{Defer reproduction control to the
personality module's age windows.}
\item{\code{max_age = 999L}}{Defer death control to the personality module
(year-2 reproduction kills the parent).}
\item{\code{grid_rows}/\code{grid_cols = 30L}}{Standard density (Wolf used a
non-spatial population; clade needs a grid large enough for
one-per-cell + neighborhood encounters).}
\item{\code{n_agents_init = 60L}}{Initial density ~7\%.}
\item{\code{max_agents = 500L}}{Standard cap.}
\item{\code{max_ticks = 2000L}}{20 generations at the default
\code{wolf_year2_repro_age = 100}. Wolf used 50,000 generations for his
paper figures — for an exact reproduction, scale much higher.}
\item{\code{ploidy = 1L}}{Haploid asexual genetics, matching Wolf's basic
model (Methods §"Basic model"). The diploid quantitative-genetics
extension (Wolf's Fig 4) can be enabled by setting \code{ploidy = 2L}.}
}

Wolf-specific parameters (β, f_high, f_low, V, δ, b, γ, year ages,
per-tick game frequencies) inherit their defaults from \code{default_specs()}.
Override individual fields after calling this function:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{s <- wolf_personality_specs()
s$personality_hawkdove_radius <- 2L   # widen pairing neighborhood
s$max_ticks <- 5000L                   # longer evolution
env <- run_alife(s)
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=default_specs]{default_specs()}}, \code{\link[=run_alife]{run_alife()}}.
}
