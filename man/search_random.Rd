% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_random}
\alias{search_random}
\title{Stochastic parameter sweep for evolutionary outcome discovery}
\usage{
search_random(
  specs_base,
  search_params,
  n_samples = 50L,
  objective = "genetic_diversity",
  verbose = TRUE
)
}
\arguments{
\item{specs_base}{A specs list from \code{\link[=default_specs]{default_specs()}}.}

\item{search_params}{Named list of parameter ranges. Each element should be
a numeric vector of length 2 (\code{c(min, max)}) for uniform sampling, or a
numeric vector of length > 2 for discrete sampling. Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  mutation_sd   = c(0.01, 0.5),
  grass_rate    = c(0.05, 0.8),
  n_agents_init = c(10L, 200L)
)
}\if{html}{\out{</div>}}}

\item{n_samples}{Integer. Number of random configurations to evaluate
(default \code{50L}).}

\item{objective}{Character or function. Column from \code{get_run_data()$ticks}
to maximise (default \code{"genetic_diversity"}), or a function \code{f(env)}
returning a numeric scalar.}

\item{verbose}{Logical. Print progress (default \code{TRUE}).}
}
\value{
A data frame with one row per sample, columns:
\describe{
\item{\code{rank}}{Rank by descending objective score (1 = best).}
\item{\code{score}}{Objective value for this sample.}
\item{...}{One column per element of \code{search_params} showing the sampled
value used.}
}
Attribute \code{"specs_list"} is a list of the full specs for each sample row,
accessible via \code{attr(result, "specs_list")}.
}
\description{
Evaluates \code{n_samples} randomly drawn parameter configurations and returns
the results ranked by the chosen objective. Each sample is obtained by
independently perturbing each element of \code{search_params} (log-scale for
positive parameters, linear scale otherwise). This provides an inexpensive
exploration baseline comparable to a Latin Hypercube Sampling (LHS) sweep.
}
\details{
Use \code{search_random()} to:
\itemize{
\item Screen which parameters most strongly influence genetic diversity.
\item Identify high-diversity corners of the parameter space before running
the more expensive \code{\link[=search_map_elites]{search_map_elites()}} or \code{\link[=search_cmaes]{search_cmaes()}}.
\item Compare stochastic results to MAP-Elites elites to spot archive gaps.
}
}
\examples{
\dontrun{
# Screen mutation_sd and grass_rate for highest genetic diversity
result <- search_random(
  specs_base    = default_specs(),
  search_params = list(
    mutation_sd   = c(0.01, 0.5),
    grass_rate    = c(0.05, 0.8),
    n_agents_init = c(10L, 150L)
  ),
  n_samples  = 30L,
  objective  = "genetic_diversity"
)
head(result)                          # top configurations
plot(result$grass_rate, result$score) # partial dependence
}

}
\seealso{
\code{\link[=search_map_elites]{search_map_elites()}}, \code{\link[=search_cmaes]{search_cmaes()}}, \code{\link[=search_gradient]{search_gradient()}}
}
