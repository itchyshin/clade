% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{estimate_heritability}
\alias{estimate_heritability}
\title{Estimate narrow-sense heritability from a logged trait time-series}
\usage{
estimate_heritability(run_data, trait = "body_size")
}
\arguments{
\item{run_data}{A list returned by \code{\link[=get_run_data]{get_run_data()}} (must contain
\verb{$ticks} with the column \code{paste0("mean_", trait)}).}

\item{trait}{Character. The trait name (without the \code{mean_} prefix).
Defaults to \code{"body_size"}. Any column of the form \verb{mean_<trait>} in
\code{run_data$ticks} is supported (e.g. \code{"immune_strength"},
\code{"metabolic_rate"}, \code{"learning_rate"}).}
}
\value{
A list with components:
\describe{
\item{\verb{$h2}}{Numeric. Lag-1 autocorrelation of \verb{mean_<trait>}. Returns
\code{NA_real_} if the series has fewer than three usable values or zero
variance.}
\item{\verb{$method}}{Character constant \code{"lag1_autocorrelation"}.}
\item{\verb{$trait}}{The trait name supplied by the caller.}
\item{\verb{$n}}{Integer. Number of paired observations used.}
\item{\verb{$note}}{Character. A reminder that this is a proxy and that an
exact estimate requires parent-offspring logging.}
}

Note that clade also exports \code{\link[=heritability_estimate]{heritability_estimate()}} — a different
function that computes h^2 by parent-offspring regression on
\code{get_run_data(env)$deaths} (the agent-death log), and so requires that
agents have died with recorded \code{parent_id} and the trait of interest.
\code{estimate_heritability()} is the population-level autocorrelation proxy
(works on any logged trait series); \code{heritability_estimate()} is the
individual-level regression (requires the deaths data frame).
}
\description{
\code{estimate_heritability()} returns a coarse estimate of narrow-sense
heritability (\eqn{h^2}) for a quantitative trait that has been logged
once per tick by \code{\link[=run_alife]{run_alife()}}. The estimator is the lag-1 temporal
autocorrelation of the population mean,
\deqn{\hat{h}^2 \approx \mathrm{cor}(\bar{z}_t,\, \bar{z}_{t+1}),}
which is used here as a \emph{proxy} for the parent-offspring regression
(Falconer & Mackay 1996, ch. 10). The proxy is reasonable when (i) the
trait is under directional or stabilising selection, (ii) generation
overlap is moderate, and (iii) the logging interval is short relative to
the generation time. It is \strong{not} an exact quantitative-genetic estimate
and should not be reported as one. An exact estimate requires
parent-offspring pairs, which clade does not currently log.
}
\examples{
\dontrun{
env  <- run_alife(default_specs())
rd   <- get_run_data(env)
estimate_heritability(rd, trait = "body_size")
estimate_heritability(rd, trait = "immune_strength")
}

}
\references{
Falconer, D.S. & Mackay, T.F.C. (1996) \emph{Introduction to Quantitative
Genetics}, 4th ed. Longman, Harlow.
}
\seealso{
\code{\link[=heritability_estimate]{heritability_estimate()}} for the parent-offspring regression
approach. \code{\link[=get_run_data]{get_run_data()}}, \code{\link[=compute_ld]{compute_ld()}}, \code{\link[=species_tree]{species_tree()}}.
}
